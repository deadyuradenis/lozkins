const { dirs, globs, watchingGlobs } = require('./const.js');
const { alias } = require('./alias.js');
const { scripts } = require('./scripts.js');

module.exports = {
  dirs,
  globs,
  watchingGlobs,
  alias,
  scripts,
};
