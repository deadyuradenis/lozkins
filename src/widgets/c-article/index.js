import './index.scss';

const cArticle = (componentNode) => {
    const component = componentNode;

    if (!component) {
        // console.debug()
        return;
    }

};

export default cArticle;
