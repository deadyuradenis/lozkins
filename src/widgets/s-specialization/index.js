import './index.scss';

const sSpecialization = (componentNode) => {
    const component = componentNode;

    if (!component) {
        // console.debug()
        return;
    }

};

export default sSpecialization;
