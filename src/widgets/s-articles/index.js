import './index.scss';

const sArticles = (componentNode) => {
    const component = componentNode;

    if (!component) {
        // console.debug()
        return;
    }

};

export default sArticles;
