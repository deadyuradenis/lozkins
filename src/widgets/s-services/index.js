import './index.scss';

const sServices = (componentNode) => {
    const component = componentNode;

    if (!component) {
        // console.debug()
        return;
    }

};

export default sServices;
